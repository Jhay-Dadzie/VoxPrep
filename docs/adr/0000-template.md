# ADR-0000: Title as a statement, not a topic

- **Status:** Proposed
- **Date:** YYYY-MM-DD
- **Deciders:** who agreed to this
- **Supersedes:** ADR-NNNN *(omit if none)*
- **Superseded by:** ADR-NNNN *(added later, if it happens)*

## Context

The situation that forced a decision. Include the constraints that were real at
the time — vendor limits, platform behaviour, deadlines, existing data — because
those are what a future reader cannot reconstruct.

State the problem, not the solution.

## Decision

What was decided, in the active voice: "We do X." One decision per record.

## Options considered

### Option A — the one chosen

Why it wins.

### Option B — rejected

What it would have given, and why that was not enough. **This section is the
part a future reader most needs**: without it, the rejected option gets
proposed again.

## Consequences

### Positive

- What this buys.

### Negative

- What it costs. An ADR with no negative consequences is advertising, not a
  record.

### Neutral

- Things that are simply now true as a result.

## Implementation

Where this decision lives in the code:

- `path/to/file.js`
- `path/to/other.ts`

## Revisiting

What would have to change for this decision to be worth reopening.
